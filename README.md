# Oryna Goichuk - Final Year Project - EstateMate
AI-Powered Area & Property Finder

## How to Run

### Backend
* Run `pip install requirements.txt`
* Run `python manage.py runserver`

### Frontend
* Run `cd frontend`
* Run `npm install`
* Run `npm run start`
